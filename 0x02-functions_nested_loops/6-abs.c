#include "main.h"
#include <stdio.h>
/**
 * main - check the code
 *int_abs - function that computes the absolute value
 * @n: takes integer type
 * Return: Always 0.
 */
int _abs(int)
{
if (n < 0)
return (-n);
else
{
return (n);
}
}

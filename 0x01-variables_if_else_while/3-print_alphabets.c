#include <unistd.h>
#include <stdio.h>
/**
* main - Entry point
* Description: c program
* Return: always 0 (Success)
*/
int main(void)
{
	char ch = 'a';
	char CH = 'A';

	while (ch <= 'z')
		putchar(ch);
	ch++;
}
while (CH <= 'Z')
putchar(CH);
CH++;
}
return (0);
}

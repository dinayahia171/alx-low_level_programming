#include "main.h"
#include <stdio.h>
/**
 * main - check the code
 * _abs- function that computes the absolute value
 * @n: takes integer type
 * Return: Always 0.
 */
int _abs(int n)
{
if (n < 0)
	n = (-1) * n;
return (n);
}
